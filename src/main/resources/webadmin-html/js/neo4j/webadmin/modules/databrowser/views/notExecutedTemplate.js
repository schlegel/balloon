(function(define){
define(function(){return function(vars){
with(vars||{}) {
return "<p><b>Query not executed yet.</b> Press the search button or hit <i>CTRL+ENTER</i> inside the query editor to execute it.</p>"; 
}};
});})(typeof define=="function"?
define:
function(factory){module.exports=factory.apply(this, deps.map(require));});
